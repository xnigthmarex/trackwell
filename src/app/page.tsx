import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      this will be our landing page which will redirect to /dashboard url which will check for auth if not redirect to auth page and then start the 3d background
    </main>
  );
}
